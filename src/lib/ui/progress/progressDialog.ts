import { Dialog } from '../dialog';
import { Component } from 'vue-property-decorator';

@Component({})
export default class ProgressDialog extends Dialog {
  private name: string = 'progressDialog';
}
